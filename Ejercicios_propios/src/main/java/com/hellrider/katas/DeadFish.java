package com.hellrider.katas;

/**
 * Crea un analizador para interpretar y ejecutar el lenguaje Deadfish.
 *
 * Deadfish opera sobre un único valor en la memoria, que inicialmente está establecido en 0.
 *
 * Utiliza cuatro comandos de un solo carácter: i:
 * Incrementar el valor d:
 * Decrementar el valor s:
 * Elevar al cuadrado el valor o:
 *
 * Imprime el valor en una matriz de resultados Todas las demás instrucciones no son operaciones y no tienen efecto.
 *
 * Ejemplos El programa "iiisdoso" debe devolver números [8, 64].
 * El programa "iiisdosodddddiso" debe devolver números [8, 64, 3600].
 * */

public class DeadFish {

    public static int[] parse(String data) {
        return new int[0];
    }
}
